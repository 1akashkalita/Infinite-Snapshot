import { describe, expect, it } from "vitest";

// Smoke test: proves the Vitest harness is wired up and assertions run.
// Replace or extend with real unit tests as the app grows.
describe("test harness", () => {
  it("runs and evaluates assertions", () => {
    expect(1 + 1).toBe(2);
  });
});
