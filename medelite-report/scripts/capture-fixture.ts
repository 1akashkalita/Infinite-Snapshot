// Fixture capture entrypoint, run via `npm run fixture:capture` (tsx).
//
// Placeholder: wire real fixture capture here later (e.g. snapshot API
// responses or rendered output into a fixtures/ directory). Kept as a typed,
// exported no-op so the script is runnable today and stays green under
// typecheck/lint. The `export` also satisfies `isolatedModules`.
export function captureFixtures(): void {
  console.log("No fixtures configured yet.");
}

captureFixtures();
