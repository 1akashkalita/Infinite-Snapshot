---
phase: 03-web-ui-core-flow-deployment
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - medelite-report/src/components/ManualInputsForm.tsx
  - medelite-report/src/components/SnapshotApp.tsx
  - medelite-report/src/components/ReportPreview.tsx
autonomous: true
requirements: [INPT-01, INPT-02, INPT-03, PREV-01, NAME-02]
user_setup: []

must_haves:
  truths:
    - "User can fill all six manual fields (EMR, Current Census, Type of Patient, Medical Coverage, Previous Provider Performance, Previous Coverage from Medelite Yes/No) and see them reflected in the preview instantly (INPT-01/02/03, PREV-01)"
    - "User can override the facility name and see only the report body 'Name of Facility' update — the static header block is unaffected (NAME-02, rule #2)"
    - "Editing a manual input updates the preview on every keystroke with no re-fetch of CMS data (PREV-01)"
    - "Manual inputs are disabled before the first successful fetch and reset on a new successful fetch (D-11)"
  artifacts:
    - path: "medelite-report/src/components/ManualInputsForm.tsx"
      provides: "Controlled form for the six manual fields + name override bound to ManualInputs"
      min_lines: 40
    - path: "medelite-report/src/components/SnapshotApp.tsx"
      provides: "ManualInputsForm wired to manualInputs state (onChange → setManualInputs)"
      contains: "ManualInputsForm"
    - path: "medelite-report/src/components/ReportPreview.tsx"
      provides: "Body renders the bound manual fields + the override-aware displayName"
  key_links:
    - from: "medelite-report/src/components/ManualInputsForm.tsx"
      to: "medelite-report/src/components/SnapshotApp.tsx"
      via: "onChange(next) → setManualInputs → re-render"
      pattern: "onChange"
    - from: "medelite-report/src/components/SnapshotApp.tsx"
      to: "medelite-report/src/lib/report/view-model.ts"
      via: "assembleViewModel re-runs on every manualInputs change (PREV-01)"
      pattern: "assembleViewModel\\("
    - from: "medelite-report/src/components/ManualInputsForm.tsx"
      to: "medelite-report/src/lib/report/view-model.ts"
      via: "ManualInputs interface (the six fields + nameOverride) to bind"
      pattern: "ManualInputs"
---

<objective>
Complete the phase: bind the six manual operational inputs and the facility-name override, with the
live preview updating on every keystroke.

This plan adds `ManualInputsForm` — controlled inputs for all six manual fields (EMR, Current Census,
Type of Patient, Medical Coverage, Previous Provider Performance, Previous Coverage Yes/No) plus the
optional name override (D-12 field types) — wires it into SnapshotApp's `manualInputs` state so every
edit re-runs `assembleViewModel` and re-renders the preview (PREV-01), and finishes ReportPreview's
body so the bound manual values and the override-aware `displayName` render. NAME-02 is already enforced
in `assembleViewModel` (`displayName = nameOverride?.trim() || providerName`) and the static header is
untouched (rule #2) — this plan just binds the field and confirms the isolation.

After this plan a user can do something they could not before: enter operational data and a custom name
and watch the snapshot update live.

Purpose: ROADMAP success criteria #3 (manual inputs reflected) and #4 (name override, body-only).
Output: ManualInputsForm + the wired SnapshotApp + the completed ReportPreview body.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-ui-core-flow-deployment/03-CONTEXT.md
@.planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md
@.planning/phases/03-web-ui-core-flow-deployment/03-RESEARCH.md

# CLAUDE.md standing rules + branding rule #2 (header static); CHECKLIST.md field types
@CLAUDE.md
@medelite-report/AGENTS.md

<interfaces>
<!-- The ManualInputs shape to bind (extended in Plan 01 with previousProviderPerformance). -->

From medelite-report/src/lib/report/view-model.ts (ManualInputs — post Plan 01):
  export interface ManualInputs {
    nameOverride?: string;                       // text — body "Name of Facility" only (NAME-02)
    emr?: string;                                // text
    currentCensus?: number | null;              // numeric (the ONLY numeric field, D-12)
    typeOfPatient?: string;                       // text
    medicalCoverage?: string;                     // free text
    previousProviderPerformance?: string;         // text (added in Plan 01, INPT-01 sixth field)
    previousCoverage?: "Yes" | "No" | null;      // Yes/No select (CHECKLIST.md)
  }
  // assembleViewModel already maps all of these into vm.manual.* and vm.facility.displayName.
  // displayName = manual.nameOverride?.trim() || facility.providerName (NAME-02 — body only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ManualInputsForm — six controlled fields + name override (D-12 types)</name>
  <files>medelite-report/src/components/ManualInputsForm.tsx</files>
  <read_first>
    - .planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md (the "ManualInputsForm" section lines 249-315 — props, controlled merge-update onChange, the numeric Current Census field, the Yes/No select)
    - medelite-report/src/lib/report/view-model.ts (the ManualInputs interface — bind every field including the new previousProviderPerformance)
    - CLAUDE.md (field-mapping table — Current Census numeric, Previous Coverage Yes/No, Medical Coverage its own free-text field, name override body-only)
    - .planning/phases/03-web-ui-core-flow-deployment/03-CONTEXT.md (D-12 field types; D-11 disabled-before-first-fetch)
  </read_first>
  <action>
    Create `medelite-report/src/components/ManualInputsForm.tsx` with `'use client'` first line. Import
    `type ManualInputs` from `@/lib/report/view-model`. Props:
    `{ inputs: ManualInputs; onChange: (next: ManualInputs) => void; disabled: boolean }`. Render
    controlled inputs for all six manual fields PLUS the name override (seven controls total). Each text
    field uses the merge-update pattern `onChange({ ...inputs, <field>: e.target.value })` so one field
    never clobbers another, with `value={inputs.<field> ?? ''}`: `nameOverride` (text, label "Name
    Override"), `emr` (text), `typeOfPatient` (text), `medicalCoverage` (free text), and
    `previousProviderPerformance` (text). `currentCensus` is the ONLY numeric field (`<input
    type="number">`, `value={inputs.currentCensus ?? ''}`, `onChange={e => onChange({ ...inputs,
    currentCensus: e.target.valueAsNumber || null })}` — D-12). `previousCoverage` is a Yes/No
    `<select>` (options "" / "Yes" / "No"; on change set `v === 'Yes' || v === 'No' ? v : null` so the
    type stays `"Yes" | "No" | null`). All controls receive `disabled={disabled}` (D-11 — disabled
    until the first successful fetch). Use Tailwind v4 utilities for a simple stacked form (label +
    input per row). Named export; import nothing Node-only.
  </action>
  <verify>
    <automated>cd medelite-report && npx tsc --noEmit && grep -q 'type="number"' src/components/ManualInputsForm.tsx && grep -q "previousProviderPerformance" src/components/ManualInputsForm.tsx && grep -q "previousCoverage" src/components/ManualInputsForm.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `medelite-report/src/components/ManualInputsForm.tsx` exists, first line `'use client'`, imports `ManualInputs` from `@/lib/report/view-model`
    - All six manual fields + `nameOverride` are bound as controlled inputs; `grep -cE 'nameOverride|emr|currentCensus|typeOfPatient|medicalCoverage|previousProviderPerformance|previousCoverage' src/components/ManualInputsForm.tsx` returns at least 7
    - `currentCensus` uses `<input type="number">` and is the only numeric input; `previousCoverage` is a `<select>` with Yes/No
    - Every control receives `disabled={disabled}` (D-11)
    - `cd medelite-report && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ManualInputsForm binds all six manual fields + name override with the correct D-12 types (numeric census, Yes/No select), merge-updates without clobbering, and disables until first fetch.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ManualInputsForm into SnapshotApp + finish ReportPreview body (live preview, NAME-02)</name>
  <files>medelite-report/src/components/SnapshotApp.tsx, medelite-report/src/components/ReportPreview.tsx</files>
  <read_first>
    - medelite-report/src/components/SnapshotApp.tsx (the Wave-3 wired app — manualInputs state already exists; add the form)
    - medelite-report/src/components/ReportPreview.tsx (the Wave-3 body — replace the em-dash manual placeholders with the bound values)
    - medelite-report/src/lib/report/view-model.ts (assembleViewModel maps manual.* into vm.manual and the override into vm.facility.displayName — NAME-02 is already enforced here)
    - .planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md (the SnapshotApp two-pane layout lines 159-171 and the ReportPreview body lines 319-360)
    - medelite-report/tests/lib/report/view-model.test.ts (proves displayName uses the override and the static header is unaffected — the NAME-02 isolation the UI relies on)
  </read_first>
  <action>
    In `medelite-report/src/components/SnapshotApp.tsx`, import `ManualInputsForm` and render it in the
    left pane beneath `CCNSearchBar`:
    `<ManualInputsForm inputs={manualInputs} onChange={setManualInputs} disabled={!facilityData} />`
    (disabled until a facility is loaded, D-11). The existing `vm = facilityData ?
    assembleViewModel(facilityData, manualInputs, new Date()) : null` already re-runs on every
    `manualInputs` change because `setManualInputs` triggers a re-render — this is the live-preview wire
    (PREV-01); do NOT add a debounce (Phase 7). Do NOT re-fetch CMS data on manual edits (only
    `handleSearch` fetches). In `medelite-report/src/components/ReportPreview.tsx`, replace the Wave-3
    em-dash placeholders in the body `<dl>` with the bound manual values from `vm.manual`: EMR
    (`vm.manual.emr`), Current Census (`vm.manual.currentCensus`, render `String(...)` when not null else
    an em-dash), Type of Patient (`vm.manual.typeOfPatient`), Medical Coverage
    (`vm.manual.medicalCoverage`), Previous Provider Performance (`vm.manual.previousProviderPerformance`),
    Previous Coverage from Medelite (`vm.manual.previousCoverage`) — each with an em-dash fallback when
    absent. Confirm "Name of Facility" renders `vm.facility.displayName` (which already honors the
    override, NAME-02) while the header block keeps rendering the STATIC `vm.header.*` strings (rule #2 —
    the override must NOT change the header). All values via React JSX (auto-escaped); no
    `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>cd medelite-report && npm run verify:full</automated>
  </verify>
  <acceptance_criteria>
    - SnapshotApp renders `<ManualInputsForm inputs={manualInputs} onChange={setManualInputs} disabled={!facilityData} />`
    - SnapshotApp does NOT call `fetch` from the manual-input change path (only `handleSearch` fetches) — preview updates come from the re-run of `assembleViewModel`, no debounce
    - ReportPreview body renders `vm.manual.emr`/`currentCensus`/`typeOfPatient`/`medicalCoverage`/`previousProviderPerformance`/`previousCoverage` and `vm.facility.displayName`
    - ReportPreview header still renders the static `vm.header.platformLine`/`reportTitle`/`stateLine` (NAME-02 isolation — override does not touch the header)
    - `cd medelite-report && npm run verify:full` exits 0
  </acceptance_criteria>
  <done>ManualInputsForm is wired to manualInputs; the preview updates live on every keystroke with no re-fetch; the body shows all six manual fields + the override-aware displayName while the static header is unaffected; verify:full is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual input fields → preview body | User strings (incl. name override) rendered into the report body via React JSX |
| manual inputs → assembleViewModel | Local-only client state; no server interaction on manual edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering (XSS) | name override + manual fields rendered in ReportPreview | mitigate | All manual values (including `nameOverride` → displayName) rendered via React JSX interpolation (auto-escaped); NO `dangerouslySetInnerHTML`; the override flows only to the body displayName, never to the static header (NAME-02 / rule #2) |
| T-03-11 | Tampering | currentCensus numeric coercion | mitigate | `e.target.valueAsNumber || null` keeps the field `number | null`; a non-numeric entry resolves to null rather than NaN propagating into the model |
| T-03-SC | Tampering | npm installs | accept | No package installs in this plan (Phase 3 adds zero new packages) |
</threat_model>

<verification>
Run from `medelite-report/`:
- `npm run verify:full` green (typecheck + lint + format + tests + `next build`)
- Manual: with CCN 686123 loaded, typing in each manual field updates the preview body instantly; setting a name override changes only "Name of Facility" while the static header is unchanged; no network request fires on manual edits
- After push, re-confirm the live Vercel URL returns 200 (`curl -sI <vercel-url>`) — phase gate DEP-01
</verification>

<success_criteria>
- All six manual fields + Yes/No control are fillable and reflected in the preview instantly (INPT-01/02/03, PREV-01, ROADMAP SC#3)
- Name override updates only the body "Name of Facility"; the static header block is unaffected (NAME-02, rule #2, ROADMAP SC#4)
- Manual edits update the preview with no re-fetch of CMS data (PREV-01)
- `npm run verify:full` is green; the live Vercel URL returns 200 (phase gate)
</success_criteria>

<output>
Create `.planning/phases/03-web-ui-core-flow-deployment/03-04-SUMMARY.md` when done
</output>
