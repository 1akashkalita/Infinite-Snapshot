---
phase: 03-web-ui-core-flow-deployment
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - medelite-report/src/app/layout.tsx
  - medelite-report/src/app/page.tsx
  - medelite-report/src/components/SnapshotApp.tsx
autonomous: false
requirements: [DEP-01, DEP-02]
user_setup:
  - service: vercel
    why: "Deploy the Next.js app (in the medelite-report/ subdir) to a live public URL (DEP-01)"
    dashboard_config:
      - task: "Add New Project → import github.com/1akashkalita/Infinite-Snapshot → set Root Directory = medelite-report → enable auto-deploy on main + PR previews → Deploy. No environment variables (D-17)."
        location: "Vercel Dashboard → Add New Project"

must_haves:
  truths:
    - "page.tsx is a thin server component that renders <SnapshotApp />"
    - "SnapshotApp is a 'use client' component that renders a paper-like skeleton on first load (D-06/D-16)"
    - "The app builds with `next build` and the production build is green"
    - "The app is live at a public Vercel URL returning HTTP 200 (DEP-01)"
    - "All local commits are pushed to the public github.com/1akashkalita/Infinite-Snapshot (DEP-02)"
  artifacts:
    - path: "medelite-report/src/app/page.tsx"
      provides: "Thin server shell rendering the client app"
      contains: "SnapshotApp"
    - path: "medelite-report/src/components/SnapshotApp.tsx"
      provides: "Client app shell with two-pane layout + skeleton preview"
      min_lines: 20
    - path: "medelite-report/src/app/layout.tsx"
      provides: "Infinite Snapshot metadata title"
      contains: "Infinite Snapshot"
  key_links:
    - from: "medelite-report/src/app/page.tsx"
      to: "medelite-report/src/components/SnapshotApp.tsx"
      via: "import + JSX render"
      pattern: "import.*SnapshotApp"
    - from: "Vercel project"
      to: "github.com/1akashkalita/Infinite-Snapshot"
      via: "dashboard Git integration, Root Directory = medelite-report"
      pattern: "medelite-report"
---

<objective>
Ship the thinnest deployable end-to-end stack to Vercel FIRST (D-16 skeleton-first), to establish
continuous deployment and surface any subdir / build / runtime issues before the full flow lands.

This plan replaces the Next scaffold `page.tsx` with a thin server shell rendering a `"use client"`
`SnapshotApp` that draws the two-pane layout and a greyed paper-like skeleton preview (D-01/D-02/D-06),
updates the document metadata to "Infinite Snapshot", verifies a clean production build, then performs
the first Vercel deploy (D-14) — a USER action — and confirms the live URL returns 200 (DEP-01) and the
repo is public with all commits pushed (DEP-02).

Purpose: De-risk the "live URL" deliverable early; later waves add the search/fetch/preview/inputs on
top of an already-shipping base.
Output: layout metadata, thin page.tsx, SnapshotApp shell, live Vercel URL, pushed public repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-web-ui-core-flow-deployment/03-CONTEXT.md
@.planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md
@.planning/phases/03-web-ui-core-flow-deployment/03-RESEARCH.md

# CLAUDE.md standing rules (run all npm from medelite-report/); AGENTS.md Next.js 16 caveat
@CLAUDE.md
@medelite-report/AGENTS.md

<interfaces>
<!-- Existing files this plan edits/wraps. Extracted from the codebase. -->

From medelite-report/src/app/layout.tsx (current metadata — lines 15-18, update title/description only):
  export const metadata: Metadata = {
    title: "Create Next App",                       // → "Infinite Snapshot"
    description: "Generated by create next app",    // → app description
  };

From medelite-report/src/app/page.tsx — currently the Next.js scaffold (Image + marketing copy).
Target: a zero-hook server component that imports and renders <SnapshotApp />.

Tailwind v4 (globals.css uses `@import "tailwindcss"`) — NO tailwind.config.js (RESEARCH Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout metadata + replace page.tsx scaffold with a thin server shell</name>
  <files>medelite-report/src/app/layout.tsx, medelite-report/src/app/page.tsx</files>
  <read_first>
    - medelite-report/src/app/layout.tsx (lines 15-18 — the metadata object to update; keep the font + body structure intact)
    - medelite-report/src/app/page.tsx (the current scaffold to fully replace)
    - .planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md (the "src/app/page.tsx" section lines 30-66 and the "layout.tsx metadata" section lines 70-98)
    - medelite-report/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (the server-renders-client-tree pattern — page stays a server component, all interactivity lives in the client component)
  </read_first>
  <action>
    In `medelite-report/src/app/layout.tsx`, change `metadata.title` to `"Infinite Snapshot"` and
    `metadata.description` to a one-line app description (e.g. "Generate a polished nursing-home
    facility assessment snapshot from a CMS CCN."). Do NOT touch the font setup, `<html>`/`<body>`
    classes, or `RootLayout`. Replace the entire body of `medelite-report/src/app/page.tsx` with a
    thin server component: remove the `next/image` import and all scaffold markup, import
    `{ SnapshotApp } from "@/components/SnapshotApp"`, and render `<SnapshotApp />` from the default
    `export default function Home()`. Do NOT add `"use client"` to page.tsx — it stays a server
    component (RESEARCH Pitfall 6). The file must keep its default export (isolatedModules).
  </action>
  <verify>
    <automated>cd medelite-report && grep -q "Infinite Snapshot" src/app/layout.tsx && grep -q "SnapshotApp" src/app/page.tsx && ! grep -q "use client" src/app/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Infinite Snapshot" medelite-report/src/app/layout.tsx` succeeds (title updated)
    - `medelite-report/src/app/page.tsx` imports and renders `SnapshotApp` and contains NO `next/image` import
    - `medelite-report/src/app/page.tsx` does NOT contain `"use client"`
  </acceptance_criteria>
  <done>layout metadata reads "Infinite Snapshot"; page.tsx is a thin server shell rendering <SnapshotApp /> with no scaffold markup and no "use client".</done>
</task>

<task type="auto">
  <name>Task 2: Create SnapshotApp client shell — two-pane layout + skeleton preview; verify production build</name>
  <files>medelite-report/src/components/SnapshotApp.tsx</files>
  <read_first>
    - .planning/phases/03-web-ui-core-flow-deployment/03-PATTERNS.md (the "src/components/SnapshotApp.tsx" section lines 102-188 for the two-pane layout + 'use client' boundary, and the "ReportPreview skeleton" pattern lines 362-382)
    - medelite-report/src/app/globals.css (the Tailwind v4 `@import "tailwindcss"` setup — no tailwind.config.js, RESEARCH Pitfall 5)
    - medelite-report/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md (the 'use client' module-graph rules)
  </read_first>
  <action>
    Create `medelite-report/src/components/SnapshotApp.tsx` with `'use client'` as the first line.
    For THIS plan it is a minimal shippable shell — no fetch, no manual inputs yet (those land in
    Waves 3-4). Render the two-pane layout (D-01): `<div className="flex flex-col lg:flex-row gap-6 p-6">`
    with a left pane (a heading "Infinite Snapshot" and a placeholder for the search bar that later
    waves fill) and a right pane containing a paper-like greyed skeleton preview (D-02/D-06): a white
    "page" panel (`bg-white rounded shadow p-8`) with `animate-pulse space-y-4` placeholder rows
    (`h-6`/`h-4 bg-gray-200 rounded` divs and a divider) approximating the report layout — NOT a blank
    area, NOT just a spinner. Use only Tailwind v4 utility classes (no tailwind.config.js). Do NOT
    import `@/lib/cms/client` or anything from `@react-pdf/renderer` (RESEARCH Pitfall 1 — client-bundle
    safety). Export `SnapshotApp` as a named export (isolatedModules). After the file compiles, run the
    full production build to confirm the subdir app builds clean before deploying.
  </action>
  <verify>
    <automated>cd medelite-report && npm run verify:full</automated>
  </verify>
  <acceptance_criteria>
    - `medelite-report/src/components/SnapshotApp.tsx` exists, first line is `'use client'`, exports `SnapshotApp`
    - `grep -q "animate-pulse" medelite-report/src/components/SnapshotApp.tsx` succeeds (skeleton present, D-06)
    - `medelite-report/src/components/SnapshotApp.tsx` does NOT import `@/lib/cms/client` or `@react-pdf/renderer`
    - `cd medelite-report && npm run verify:full` exits 0 (typecheck + lint + format + tests + `next build` all green)
  </acceptance_criteria>
  <done>SnapshotApp renders a two-pane layout with a greyed paper-like skeleton preview, imports nothing Node-only, and `npm run verify:full` is green including the production build.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: First Vercel deploy + push public repo (DEP-01 / DEP-02) — USER ACTION</name>
  <what-built>
    A buildable Next.js app in `medelite-report/` whose production build (`next build`) is green
    (verified in Task 2). The Vercel one-time account connect cannot be automated by an agent (D-14);
    the steps below are the user's manual deploy + the agent's automated verification afterward.
  </what-built>
  <how-to-verify>
    Perform these steps (the agent runs the git push and the curl/gh checks; the Vercel dashboard
    connect is the user's one-time action):

    1. Push all local commits to the public repo (agent runs):
       `git push origin main`
    2. In the Vercel dashboard (USER): "Add New Project" → import
       `github.com/1akashkalita/Infinite-Snapshot` → set **Root Directory = `medelite-report`** →
       leave Build Command default (`next build`, auto-detected) and Output Directory default (`.next`)
       → add NO environment variables (the CMS API is public, D-17) → enable auto-deploy on `main` and
       PR preview deploys → click Deploy.
    3. Wait for the deploy to finish; copy the production URL Vercel assigns
       (e.g. `https://infinite-snapshot.vercel.app`).
    4. Confirm the live URL returns 200 (agent runs, substitute the real URL):
       `curl -sI <vercel-url>` → expect `HTTP/2 200` (or `HTTP/1.1 200`)
    5. Confirm the repo is public (agent runs):
       `gh repo view 1akashkalita/Infinite-Snapshot --json visibility` → expect `"visibility":"PUBLIC"`
    6. Record the live URL in the SUMMARY so later waves (and DEP-01) can re-verify it after each push.
  </how-to-verify>
  <acceptance_criteria>
    - `git push origin main` completes; `git status` shows the branch is up to date with `origin/main`
    - `curl -sI <vercel-url>` returns an HTTP 200 status line (DEP-01)
    - `gh repo view 1akashkalita/Infinite-Snapshot --json visibility` returns `PUBLIC` (DEP-02)
    - The live Vercel URL is recorded in the plan SUMMARY
  </acceptance_criteria>
  <resume-signal>Reply with the live Vercel URL (and confirm the curl 200 + gh PUBLIC checks passed), or describe any deploy/build issue Vercel reported.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repo → Vercel build | Public source is built by Vercel; no secrets involved (D-17) |
| Vercel serverless ↔ public internet | The live URL is publicly reachable; no auth (public report tool, by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Public repo includes .planning/ | accept | D-15 decision: repo is intentionally fully public including GSD artifacts; no secrets are committed (D-17 — no env vars/keys exist) |
| T-03-05 | Tampering | Client bundle (SnapshotApp import tree) | mitigate | SnapshotApp imports nothing Node-only; `@/lib/cms/client` and `@react-pdf/renderer` are never imported client-side (RESEARCH Pitfall 1) — `next build` in Task 2 catches a bad import before deploy |
| T-03-SC | Tampering | npm installs | accept | No package installs in this plan (Phase 3 adds zero new packages) |
</threat_model>

<verification>
Run from `medelite-report/`:
- `npm run verify:full` green (typecheck + lint + format + tests + `next build`)
- `curl -sI <vercel-url>` → HTTP 200 (DEP-01)
- `gh repo view 1akashkalita/Infinite-Snapshot --json visibility` → PUBLIC (DEP-02)
</verification>

<success_criteria>
- `page.tsx` is a thin server shell rendering `<SnapshotApp />`; metadata title is "Infinite Snapshot"
- `SnapshotApp` renders the two-pane layout + greyed paper-like skeleton preview (D-01/D-02/D-06)
- `npm run verify:full` is green including `next build`
- The app is live at a public Vercel URL returning 200 (DEP-01), Root Directory = `medelite-report`, no env vars
- All commits are pushed; the repo is PUBLIC (DEP-02)
</success_criteria>

<output>
Create `.planning/phases/03-web-ui-core-flow-deployment/03-02-SUMMARY.md` when done (record the live Vercel URL).
</output>
