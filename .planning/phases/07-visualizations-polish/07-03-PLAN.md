---
phase: 07-visualizations-polish
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - medelite-report/src/hooks/useDebounce.ts
  - medelite-report/src/hooks/useDebounce.test.ts
  - medelite-report/src/components/SnapshotApp.tsx
autonomous: false
requirements: [VIZ-01, VIZ-02]
user_setup: []

must_haves:
  truths:
    - "Editing a manual input updates the web preview within ~300ms (debounced), not on every keystroke (D-14, SC#3)"
    - "Manual-input edits NEVER trigger a CMS re-fetch — only the CCN search (handleSearch) fetches CMS data (D-14)"
    - "The single debounced view-model drives BOTH the preview and ExportControls, so preview and export stay consistent (RPT-02)"
    - "The deployed Vercel URL passes the full 'Looks Done But Isn't' checklist: leading-zero CCN, static PDF header, N/A suppression, font on Vercel, charts visible in opened PDF, charts in opened .docx, all error states, .docx < 4 MB, star colors correct (SC#4)"
    - "npm run verify:full is green before the live smoke and before phase sign-off"
  artifacts:
    - path: "medelite-report/src/hooks/useDebounce.ts"
      provides: "Generic useDebounce(value, delayMs) — explicit 300ms setTimeout/clearTimeout (D-14)"
      exports: ["useDebounce"]
    - path: "medelite-report/src/hooks/useDebounce.test.ts"
      provides: "Timer-semantics test: no fire before delay, fires after delay, latest value wins on rapid changes"
  key_links:
    - from: "medelite-report/src/components/SnapshotApp.tsx"
      to: "useDebounce"
      via: "const debouncedManualInputs = useDebounce(manualInputs, 300)"
      pattern: "useDebounce\\(manualInputs, 300\\)"
    - from: "medelite-report/src/components/SnapshotApp.tsx"
      to: "assembleViewModel"
      via: "vm = assembleViewModel(facilityData, debouncedManualInputs, ...)"
      pattern: "assembleViewModel\\(facilityData, debouncedManualInputs"
---

<objective>
Deliver the live-preview debounce and run the final live-Vercel polish pass — the closing slice that makes the visual upgrades feel finished and proves them on the deployed URL.

After this plan: editing a manual input updates the preview ~300ms later (debounced) with no CMS re-fetch, the same debounced view-model drives both the preview and the export buttons (so a downloaded PDF/.docx always matches what the user sees), and the deployed Vercel URL passes the full "Looks Done But Isn't" checklist (SC#4) — stars, charts in the opened PDF + .docx, suppression, leading-zero CCN, error states, and .docx size all confirmed on real artifacts.

Purpose: Closes SC#3 (300ms debounce, no re-fetch) and SC#4 (live-Vercel checklist) — the polish that gates phase completion. This depends on Plans 01+02 because the checklist verifies the stars and charts they produced.

Output: useDebounce hook + SnapshotApp wiring, plus a documented live-Vercel smoke pass and the verify:full phase gate.

NOTE: `autonomous: false` — Task 2 is a `checkpoint:human-verify` for the debounce-timing/no-refetch behavior, and Task 3 is the `checkpoint:human-verify` live-Vercel SC#4 checklist (open the real PDF + .docx, which automated tests cannot do).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@medelite-report/AGENTS.md
@.planning/phases/07-visualizations-polish/07-CONTEXT.md
@.planning/phases/07-visualizations-polish/07-PATTERNS.md
@.planning/phases/07-visualizations-polish/07-RESEARCH.md
@.planning/phases/07-visualizations-polish/07-01-SUMMARY.md
@.planning/phases/07-visualizations-polish/07-02-SUMMARY.md

<interfaces>
<!-- Contracts from the codebase. Use directly. -->

From src/components/SnapshotApp.tsx ("use client"):
```typescript
import { useState, useCallback } from "react"; // add useDebounce import
const [manualInputs, setManualInputs] = useState<ManualInputs>({});
// handleSearch (lines 68–133) is the ONLY CMS fetch trigger — DO NOT touch it.
// Current vm assembly (lines 138–140):
const vm = facilityData
  ? assembleViewModel(facilityData, manualInputs, new Date(), hospMetrics)
  : null;
// ManualInputsForm stays bound to raw manualInputs/setManualInputs (immediate input feedback).
```

From 07-RESEARCH.md Pattern 7 / "State of the Art": use an explicit setTimeout/clearTimeout hook, NOT useDeferredValue — useDeferredValue has no guaranteed minimum delay in React 19 concurrent mode (sub-1ms possible), useless for a precise 300ms requirement.

Live deploy: push to `main` auto-deploys to https://infinite-snapshot.vercel.app (Vercel Root Directory = medelite-report). Test facility CCN 686123 (Kendall Lakes, FL, overall_rating=4 → green stars).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useDebounce hook + SnapshotApp wiring (D-14, no CMS re-fetch)</name>
  <read_first>
    - medelite-report/src/components/SnapshotApp.tsx (the vm assembly lines 138–140; handleSearch 68–133 — must stay untouched; ManualInputsForm binding 187–191; useState/useCallback import line 29)
    - medelite-report/src/components/ManualInputsForm.tsx (the onChange path; the top-of-file comment flagging "Phase 7 adds debounce")
    - 07-RESEARCH.md Pattern 7 (useDebounce) + "Why not useDeferredValue" + Anti-Patterns ("Debounce on facilityData re-triggers CMS re-fetch")
    - 07-PATTERNS.md sections "src/hooks/useDebounce.ts" + "src/components/SnapshotApp.tsx (MODIFY)"
  </read_first>
  <behavior>
    - useDebounce(value, 300): the returned value does NOT change for <300ms after `value` changes; it updates to the latest `value` ~300ms after the last change.
    - On rapid successive changes, only the final value is emitted after the quiet period (clearTimeout on each change — latest wins).
    - useDebounce(initialValue) returns initialValue immediately on first render.
    - After wiring: `vm` is assembled from `debouncedManualInputs` (not raw `manualInputs`); `ManualInputsForm` still binds raw `manualInputs`/`setManualInputs` for immediate field feedback; `handleSearch` (CMS fetch) is byte-for-byte unchanged.
  </behavior>
  <action>
    Create `src/hooks/useDebounce.ts`: generic `useDebounce<T>(value: T, delayMs: number): T` using `useState` + `useEffect` — set a `setTimeout(() => setDebounced(value), delayMs)` and return `clearTimeout` in the effect cleanup; deps `[value, delayMs]` (Pattern 7). Do NOT use `useDeferredValue` (no guaranteed delay).

    Wire `SnapshotApp.tsx`: import `useDebounce` from `@/hooks/useDebounce`; add `const debouncedManualInputs = useDebounce(manualInputs, 300);` and change the vm assembly to `assembleViewModel(facilityData, debouncedManualInputs, new Date(), hospMetrics)`. Leave `ManualInputsForm` bound to raw `manualInputs`/`setManualInputs` (immediate UI feedback) and leave `handleSearch` and all fetch logic untouched — the debounce applies ONLY to the manualInputs→vm path, never to CMS fetching (D-14 anti-pattern guard). Update the top-of-file comment + the ManualInputsForm "Phase 7 adds debounce" comment to reflect the wiring.

    Write `src/hooks/useDebounce.test.ts` (node env, `.test.ts`) using Vitest fake timers (`vi.useFakeTimers()`): assert no update before 300ms, update after `vi.advanceTimersByTime(300)`, and latest-value-wins on rapid changes. (Test the hook's timer logic directly without a DOM by invoking the effect via a minimal harness, or test an extracted `scheduleDebounce` core if jsdom is unavailable — keep it `.test.ts`, node env, no jsdom.)
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run src/hooks/useDebounce.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useDebounce(manualInputs, 300)" medelite-report/src/components/SnapshotApp.tsx` matches; `grep -n "assembleViewModel(facilityData, debouncedManualInputs" medelite-report/src/components/SnapshotApp.tsx` matches.
    - `grep -c "useDeferredValue" medelite-report/src/hooks/useDebounce.ts` returns 0 (explicit timer, not useDeferredValue).
    - `useDebounce.test.ts` passes the fake-timer cases (no fire <300ms, fires at 300ms, latest wins).
    - The handleSearch fetch block in SnapshotApp.tsx is unchanged (diff touches only the import, the debounced line, the vm line, and comments) — no `fetch(` call added to the manualInputs path.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>useDebounce hook exists with passing timer test; SnapshotApp assembles vm from debouncedManualInputs; handleSearch/CMS fetch untouched; tsc green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 300ms debounce + no CMS re-fetch (SC#3)</name>
  <action>Pause execution and present the verification steps below to the user; resume only on "approved" or after addressing the reported issue.</action>
  <what-built>The 300ms debounce on the manual-input → preview/view-model path (Task 1). Manual edits update the preview ~300ms later and never trigger a CMS fetch; the same debounced vm drives both the preview and ExportControls.</what-built>
  <how-to-verify>
    1. `cd medelite-report && npm run dev`; open http://localhost:3000 and search CCN 686123.
    2. Open the browser DevTools Network tab, filter to `facility`.
    3. Edit a manual input (e.g. type in EMR or Type of Patient). Confirm: (a) the preview updates ~300ms after you stop typing, not on every keystroke; (b) NO new request to `/api/facility` appears in the Network tab while editing manual fields (only the original CCN search fetched CMS).
    4. Click Download PDF (or DOCX) right after an edit and confirm the exported document reflects the just-edited value (preview and export consistent — same debounced vm).
  </how-to-verify>
  <resume-signal>Type "approved" if the debounce is ~300ms with no CMS re-fetch and export matches preview, or describe what is off.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-Vercel "Looks Done But Isn't" smoke checklist (SC#4)</name>
  <action>Confirm `npm run verify:full` is green, push to main, wait for the Vercel deploy, then pause and present the 10-item checklist below to the user against the live URL; resume only when all items pass.</action>
  <what-built>The full Phase 7 visual upgrade deployed: star glyphs (web/PDF/.docx), 4 charts (web/PDF/.docx), debounce. This checkpoint runs the verbatim SC#4 checklist on the LIVE Vercel URL against real downloaded artifacts — the automated tests prove structure/values but cannot prove a chart renders non-blank in an opened PDF or that the @resvg NAPI binary loaded on Vercel Lambda.</what-built>
  <how-to-verify>
    PRE-REQ: `cd medelite-report && npm run verify:full` is green locally (typecheck + lint + format + tests + next build). Then push to `main` and wait for the Vercel deploy to finish.

    Run against https://infinite-snapshot.vercel.app (open real artifacts with poppler/a PDF reader and LibreOffice — per memory: never browser/mammoth for .docx):
    1. Leading-zero CCN: submit a CCN with a leading zero; confirm the rendered report + downloaded PDF/.docx show it correctly (no stripping).
    2. Static PDF header: download PDF; header is `INFINITE — Managed by MEDELITE` / `FACILITY ASSESSMENT SNAPSHOT` / `FL` — never the facility name (rule #2).
    3. N/A suppression: a facility with a null star rating renders grey "N/A" (no glyphs, not 0/5, not ☆☆☆☆☆) (D-06).
    4. Font on Vercel: PDF text renders as Helvetica, not a fallback (D-12/D-13).
    5. Charts visible in opened PDF: open the downloaded PDF in a real reader — 4 mini bar charts with colored bars, NOT blank rectangles (VIZ-02, Pitfall 1).
    6. Charts visible in opened .docx: open the .docx in LibreOffice — 4 chart PNG images embedded and visible (D-11).
    7. All error states: submit `000000` (invalid), a valid-format non-existent CCN, and disconnect network mid-request — three distinct clean messages (ERR-01).
    8. .docx under 4 MB: downloaded .docx file size < 4,000,000 bytes (DOCX-01, margin under the 4.5 MB Vercel limit).
    9. Star colors correct: CCN 686123 (overall_rating=4) shows green stars; confirm the band mapping (green 4–5 / amber 3 / red 1–2).
    10. @resvg on Vercel: the .docx download from the LIVE URL (not localhost) succeeds with chart images — confirms the @resvg/resvg-js linux-x64-gnu binary loaded on Vercel Lambda (Pitfall 2). If it fails with "Cannot find module / Error loading native binding", fall back to @resvg/resvg-wasm and redeploy.
  </how-to-verify>
  <resume-signal>Type "approved" when all 10 checklist items pass on the live URL with real opened artifacts, or list the failing items.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client manual-input edits → debounced vm → export routes | Debounced manual inputs flow into the same ReportViewModel that the export routes re-validate; no new trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-08 | Information Disclosure | debounce path | accept | The debounce only delays the existing client-side vm assembly; it adds no new data flow, no new endpoint, and no new validation surface. Manual inputs were already validated at the export route boundary (rule #4) — unchanged. |
| T-7-09 | Tampering | debounce never re-fetches CMS | mitigate | useDebounce wraps ONLY manualInputs→vm; handleSearch (the sole CMS fetch) is untouched. The Task-1 acceptance check (no `fetch(` on the manualInputs path) + the Task-2 Network-tab verification enforce this (D-14 anti-pattern guard). |
| T-7-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this plan (debounce is a 5-line hook; @resvg install + its legitimacy gate are in Plan 02). |
</threat_model>

<verification>
- `cd medelite-report && npm run verify:full` green before the live smoke (Task 3 pre-req) and before phase sign-off — mandatory because this phase touches the client bundle + export routes + a NAPI dependency (Plan 02).
- useDebounce timer semantics covered by useDebounce.test.ts (fake timers).
- SC#3 (debounce + no re-fetch) verified live in Task 2 (Network tab).
- SC#4 (full live-Vercel checklist) verified in Task 3 on real opened PDF + .docx artifacts.
- Regression: the full existing suite (CLM-02/CLM-03/DOCX-01/RPT-02 guards from Plans 01/02 + Phases 1–6) runs in verify:full.
</verification>

<success_criteria>
- Manual edits update the preview ~300ms later, debounced, with no CMS re-fetch (SC#3 / D-14).
- The same debounced vm drives preview + ExportControls (export matches preview, RPT-02).
- The live Vercel URL passes all 10 "Looks Done But Isn't" checklist items on real artifacts (SC#4).
- `npm run verify:full` green.
</success_criteria>

<output>
Create `.planning/phases/07-visualizations-polish/07-03-SUMMARY.md` when done.
</output>
