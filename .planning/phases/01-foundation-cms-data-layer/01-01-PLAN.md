---
phase: 01-foundation-cms-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medelite-report/package.json
  - medelite-report/package-lock.json
autonomous: false
requirements: [DATA-02]
must_haves:
  truths:
    - "All five production libraries are installed at the pinned versions with no peer-dep errors"
    - "recharts resolves to a 2.x version, not 3.x"
    - "tsc --noEmit is clean after install"
    - "npm run verify is green after install"
  artifacts:
    - path: "medelite-report/package.json"
      provides: "Five new entries under dependencies at pinned ranges"
      contains: "@react-pdf/renderer"
    - path: "medelite-report/package-lock.json"
      provides: "Resolved lockfile with recharts 2.x"
  key_links:
    - from: "medelite-report/package.json"
      to: "node_modules/zod"
      via: "npm install"
      pattern: "\"zod\""
---

<objective>
Install the five production libraries Phase 1 (and later phases) depend on, at the exact pinned versions verified in RESEARCH.md, with a peer-dependency-clean resolution against Next.js 16 / React 19, and keep `npm run verify` green.

Purpose: `zod@^4.4.3` is the runtime-validation dependency the provider schema in Plan 03 imports (CLAUDE.md rule #4). The other four (`@react-pdf/renderer`, `docx`, `recharts`, `react-pdf-charts`) are installed now per CONTEXT.md scope so later phases (4/6/7) inherit a clean, version-pinned dependency tree — in particular `recharts` MUST be pinned to v2 because `recharts@latest` is now v3.8.1 and breaks `react-pdf-charts` in Phase 7.
Output: Updated `package.json` + `package-lock.json` with the five dependencies; `npm run verify` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-cms-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-cms-data-layer/01-RESEARCH.md

<interfaces>
<!-- Verified install facts from RESEARCH.md "PRIORITY 3: Library Install & Verify-Gate". -->
<!-- Run all npm commands from medelite-report/ (CLAUDE.md). No postinstall scripts on any package. -->

Five packages to install (pinned ranges, verified clean against React 19 on 2026-06-16):
  @react-pdf/renderer@^4.5.1
  zod@^4.4.3
  docx@^9.7.1
  recharts@^2.15.4        <- MUST be ^2; recharts@latest is 3.8.1 and breaks react-pdf-charts (Phase 7)
  react-pdf-charts@^1.0.0

Current medelite-report/package.json dependencies: next 16.2.9, react 19.2.4, react-dom 19.2.4.
Current scripts include: "verify": "node scripts/verify.mjs" (typecheck -> lint -> format:check -> test).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification (blocking, not auto-approvable)</name>
  <read_first>
    - .planning/phases/01-foundation-cms-data-layer/01-RESEARCH.md (section "Package Legitimacy Audit" — slopcheck was unavailable, all five tagged [ASSUMED])
  </read_first>
  <what-built>
    Nothing is installed yet. RESEARCH.md ran a legitimacy audit on the five packages but slopcheck could not be installed (permission denied), so all five are classified `[ASSUMED]`. Per the package legitimacy gate, an `[ASSUMED]` package requires a blocking human verification before the install runs. This checkpoint is NOT auto-approvable.
  </what-built>
  <how-to-verify>
    Confirm each package is legitimate on its registry page (the maintainer/source repo and multi-year history match RESEARCH.md's audit table):
    1. https://www.npmjs.com/package/@react-pdf/renderer  (source: github.com/diegomura/react-pdf; published since 2018)
    2. https://www.npmjs.com/package/zod  (source: github.com/colinhacks/zod; published since 2020)
    3. https://www.npmjs.com/package/docx  (source: github.com/dolanmiu/docx; published since 2016)
    4. https://www.npmjs.com/package/recharts  (source: github.com/recharts/recharts; published since 2015)
    5. https://www.npmjs.com/package/react-pdf-charts  (source: github.com/EvHaus/react-pdf-charts; published since 2023)
  </how-to-verify>
  <acceptance_criteria>
    - User has reviewed all five npm package pages and confirmed they match the audit table in RESEARCH.md.
    - User explicitly approves before any `npm install` runs. Do NOT proceed on auto-advance.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize installing the five packages, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the five production libraries (pinned) and confirm the verify gate stays green</name>
  <files>medelite-report/package.json, medelite-report/package-lock.json</files>
  <read_first>
    - medelite-report/package.json (current deps/scripts — five new entries land under "dependencies")
    - .planning/phases/01-foundation-cms-data-layer/01-RESEARCH.md (section "PRIORITY 3: Library Install & Verify-Gate" — exact install command, peer-dep table, recharts v2 rationale)
    - medelite-report/scripts/verify.mjs (the gate this task must keep green)
  </read_first>
  <action>
    From `medelite-report/`, run a single install of all five packages at their pinned ranges: `@react-pdf/renderer@^4.5.1`, `zod@^4.4.3`, `docx@^9.7.1`, `recharts@^2.15.4`, `react-pdf-charts@^1.0.0` (the exact command is in RESEARCH.md PRIORITY 3). Do NOT install `recharts` without the `^2.15.4` constraint — bare `recharts` resolves to v3.8.1 (RESEARCH.md Pitfall 4). Do NOT add `serverExternalPackages` to `next.config.ts` — RESEARCH.md confirms `@react-pdf/renderer` is already on Next 16's auto-opt-out list and that config change is deferred to a later phase. Do NOT add any other dependency. After install, confirm `recharts` resolved to a 2.x version (e.g. via `npm ls recharts`) and that the install reported no peer-dependency errors. Then run the full verify gate.
  </action>
  <verify>
    <automated>cd medelite-report && npm ls recharts | grep -q "recharts@2" && npm run verify</automated>
  </verify>
  <acceptance_criteria>
    - `medelite-report/package.json` has five new keys under `dependencies`: `@react-pdf/renderer`, `zod`, `docx`, `recharts`, `react-pdf-charts`, each at the pinned range above.
    - `npm ls recharts` shows a `recharts@2.x.x` resolution (NOT 3.x).
    - The install completed with no peer-dependency error output.
    - `npm run verify` exits 0 (typecheck, lint, format:check, test all PASS).
    - `next.config.ts` is unchanged (no `serverExternalPackages` added in this phase).
  </acceptance_criteria>
  <done>All five libraries present at pinned versions, recharts is 2.x, no peer-dep errors, `npm run verify` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local node_modules | Third-party package code is downloaded and added to the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm install of the five `[ASSUMED]` packages | mitigate | Blocking, non-auto-approvable human checkpoint (Task 1) verifies each package on its npm registry page against the RESEARCH.md audit (multi-year history, known source repo) before install. RESEARCH.md confirmed none of the five has a postinstall script. |
| T-01-01 | Tampering | recharts version drift | mitigate | Pin `recharts@^2.15.4`; verify `npm ls recharts` resolves to 2.x. Prevents the v3 supply/compat regression that breaks react-pdf-charts. |

No HIGH-severity threats. No secrets, auth, PII, or network input handling in this plan — it only mutates the dependency manifest behind a human gate.
</threat_model>

<verification>
- `npm run verify` exits 0 from `medelite-report/`.
- `npm ls recharts` shows 2.x.
- `package.json` lists all five dependencies at pinned ranges.
</verification>

<success_criteria>
ROADMAP Phase 1 SC#2 satisfied: `npm install` of all five production libraries completes without peer-dep errors and `tsc --noEmit` (run inside `npm run verify`) is clean.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-cms-data-layer/01-01-SUMMARY.md` when done.
</output>
