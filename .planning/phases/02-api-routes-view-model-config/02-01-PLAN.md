---
phase: 02-api-routes-view-model-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medelite-report/src/lib/cms/constants.ts
  - medelite-report/src/lib/cms/types.ts
  - medelite-report/src/lib/cms/errors.ts
  - medelite-report/tests/lib/cms/errors.test.ts
autonomous: true
requirements: [DATA-01]
must_haves:
  truths:
    - "The 5-kind error union (invalid_ccn, not_found, network_error, cms_api_error, validation_error) exists as a single exported Zod discriminated union (D-01/D-03)"
    - "A CmsError class can be thrown and caught with instanceof, carrying a kind + message + optional extra (D-18)"
    - "assertNever produces a TypeScript compile error if a 6th kind is added to a switch without a case (D-03)"
    - "The FacilityData camelCase domain type exists with no CMS snake_case names leaked to consumers (D-14)"
    - "The CMS base URL, Provider-Information dataset id 4pq5-n9py, and CCN filter field cms_certification_number_ccn are centralized constants traced to the fixture (D-24)"
  artifacts:
    - path: "medelite-report/src/lib/cms/constants.ts"
      provides: "CMS_BASE_URL, DATASET_PROVIDER_INFO ('4pq5-n9py'), CCN_FILTER_FIELD ('cms_certification_number_ccn')"
      exports: ["CMS_BASE_URL", "DATASET_PROVIDER_INFO", "CCN_FILTER_FIELD"]
    - path: "medelite-report/src/lib/cms/types.ts"
      provides: "FacilityData curated camelCase domain interface"
      exports: ["FacilityData"]
    - path: "medelite-report/src/lib/cms/errors.ts"
      provides: "CmsApiErrorSchema discriminated union, CmsApiError type, CmsError class, assertNever"
      exports: ["CmsApiErrorSchema", "CmsApiError", "CmsError", "assertNever"]
    - path: "medelite-report/tests/lib/cms/errors.test.ts"
      provides: "CmsError construction + assertNever + discriminated-union accept/reject coverage"
  key_links:
    - from: "medelite-report/src/lib/cms/errors.ts"
      to: "zod z.discriminatedUnion('kind', ...)"
      via: "5 z.object literal kinds"
      pattern: "z\\.discriminatedUnion\\('kind'"
    - from: "medelite-report/src/lib/cms/constants.ts"
      to: "tests/fixtures/provider-686123.json field key"
      via: "CCN_FILTER_FIELD = 'cms_certification_number_ccn'"
      pattern: "cms_certification_number_ccn"
---

<objective>
Build the Phase-2 contract foundation: the three shared modules every downstream slice
consumes — the centralized CMS constants (D-24), the curated `FacilityData` domain type
(D-14), and the 5-kind error taxonomy with the `CmsError` class + `assertNever`
exhaustiveness guard (D-01/D-03/D-18). This is the interface-first wave: it defines the
contracts that the facility route (Plan 02-02) and the view-model/PDF-stub slice
(Plan 02-03) implement against, so neither has to explore the codebase to discover them.

Purpose: A single source of truth for CMS endpoint constants, the camelCase domain shape,
and the error union — so a CMS rename ripples in one place (D-14) and adding a 6th error
kind later produces a compile error in Phase 3's switch (D-03), not a silent gap.

Output: `constants.ts`, `types.ts`, `errors.ts`, and `errors.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@medelite-report/AGENTS.md
@.planning/phases/02-api-routes-view-model-config/02-CONTEXT.md
@.planning/phases/02-api-routes-view-model-config/02-PATTERNS.md

<interfaces>
<!-- Phase-1 contract this plan builds the camelCase projection ON TOP of.
     CMS snake_case field names confirmed against tests/fixtures/provider-686123.json (rule #3).
     types.ts must NOT re-export these snake_case names — it is the camelCase boundary (D-14). -->

From medelite-report/src/lib/cms/schema.ts (Phase 1 — ParsedProvider, the input the mapper will read in Plan 02-02):
```
cms_certification_number_ccn: string   // → FacilityData.ccn (string, leading zeros)
provider_name: string                  // → providerName (D-15: NOT legal_business_name)
legal_business_name: string            // (NOT mapped — has ", LLC" suffix)
provider_address: string               // → address.street
citytown: string                       // → address.city  (NOT provider_city)
state: string                          // → address.state + FacilityData.state
zip_code: string                       // (NOT in FacilityData — no ZIP per DATA-03)
processing_date: string                // → processingDate (D-12 freshness signal)
number_of_certified_beds: number|null  // → certifiedBeds
overall_rating: number|null            // → starRatings.overall
health_inspection_rating: number|null  // → starRatings.healthInspection
staffing_rating: number|null           // → starRatings.staffing
qm_rating: number|null                 // → starRatings.qualityCare (NOT longstay_/shortstay_qm_rating)
```

Zod v4 idiom (from src/lib/cms/parse.ts): use `result.error.issues` (NOT v3 `.errors`),
`z.prettifyError(result.error)` for human text. `z.discriminatedUnion('kind', [...])`
verified working on installed Zod 4.4.3 (RESEARCH.md §5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize CMS constants + the FacilityData domain type</name>
  <read_first>
    - medelite-report/scripts/capture-fixture.ts (the verified BASE URL + dataset-id registry this centralizes — analog per PATTERNS.md)
    - medelite-report/tests/fixtures/provider-686123.json (field-name anchor — rule #3; confirms `cms_certification_number_ccn` key + `4pq5-n9py` dataset)
    - medelite-report/src/lib/cms/schema.ts (the ParsedProvider snake_case keys types.ts projects to camelCase)
    - medelite-report/tsconfig.json (isolatedModules — every .ts file needs an import/export)
  </read_first>
  <files>medelite-report/src/lib/cms/constants.ts, medelite-report/src/lib/cms/types.ts</files>
  <action>
    Create `constants.ts` exporting three named constants (no class/object wrapper), each
    with a trace comment to the fixture/capture script per rule #3:
    `CMS_BASE_URL = 'https://data.cms.gov/provider-data/api/1/datastore/query'`,
    `DATASET_PROVIDER_INFO = '4pq5-n9py'` (Provider Information — the verified id, D-24),
    and `CCN_FILTER_FIELD = 'cms_certification_number_ccn'` (the verified filter field, NOT
    `federal_provider_number` which ARCHITECTURE.md sketched from memory). These are the SSRF
    containment boundary (T-02-SSRF): the host/path come only from these constants; the CCN is
    only ever a query-condition VALUE, never concatenated into the URL host or path.

    Create `types.ts` exporting the `FacilityData` interface — the curated camelCase domain
    model (D-14). Fields and their CMS sources (each annotated in a comment): `ccn: string`,
    `providerName: string` (← provider_name, D-15 — NOT legal_business_name), `address: { street; city; state }`
    (← provider_address / citytown / state — NOT provider_city/provider_state),
    `starRatings: { overall; healthInspection; staffing; qualityCare }` all `number | null`
    (← overall_rating / health_inspection_rating / staffing_rating / qm_rating — qualityCare is
    `qm_rating`, NOT longstay_qm_rating/shortstay_qm_rating, D-16), `certifiedBeds: number | null`
    (← number_of_certified_beds), `processingDate: string` (← processing_date, D-12), and a
    top-level `state: string` (← state, for `assembleHeader(state)`). No CMS snake_case name is
    re-exported from this file — it is the camelCase boundary (D-14). `export interface`/`export const`
    satisfy isolatedModules.
  </action>
  <verify>
    <automated>cd medelite-report && npx tsc --noEmit && grep -q "4pq5-n9py" src/lib/cms/constants.ts && grep -q "cms_certification_number_ccn" src/lib/cms/constants.ts && grep -Lq "federal_provider_number\|provider_city\|provider_state\|legal_business_name" src/lib/cms/types.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    `npx tsc --noEmit` clean. `constants.ts` exports `CMS_BASE_URL`, `DATASET_PROVIDER_INFO`
    (= `'4pq5-n9py'`), `CCN_FILTER_FIELD` (= `'cms_certification_number_ccn'`). `types.ts`
    exports `FacilityData` and contains NONE of the wrong/snake-case names
    (`federal_provider_number`, `provider_city`, `provider_state`, `legal_business_name`).
  </acceptance_criteria>
  <done>
    Both files exist, export their named members, typecheck clean, and field-name traceability
    (rule #3) holds — no memory-sourced field names present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Error taxonomy — discriminated union, CmsError class, assertNever (+ tests)</name>
  <read_first>
    - medelite-report/src/lib/cms/parse.ts (Zod v4 import style + `result.error.issues`/`z.prettifyError` idioms — analog)
    - medelite-report/tests/lib/cms/parse.test.ts (canonical describe/it/expect test structure to mirror)
    - .planning/phases/02-api-routes-view-model-config/02-RESEARCH.md §5 (verified Zod 4.4.3 discriminated-union + assertNever pattern)
    - medelite-report/src/lib/cms/types.ts (created Task 1 — for the `not_found` extra `ccn` field shape)
  </read_first>
  <files>medelite-report/src/lib/cms/errors.ts, medelite-report/tests/lib/cms/errors.test.ts</files>
  <behavior>
    - CmsApiErrorSchema accepts each of the 5 valid kinds (invalid_ccn, not_found w/ `ccn`, network_error, cms_api_error, validation_error)
    - CmsApiErrorSchema REJECTS an unknown kind (e.g. `{ kind: 'boom', message: 'x' }`)
    - `not_found` carries an extra `ccn: string`; the other four carry only `kind` + `message` (validation_error in particular carries NO extra detail — D-05)
    - `new CmsError('not_found', 'msg', { ccn: '686123' })` is `instanceof CmsError` AND `instanceof Error`, with `.kind === 'not_found'`, `.message === 'msg'`, `.extra.ccn === '686123'`
    - `assertNever` throws at runtime when reached with an out-of-union value
  </behavior>
  <action>
    Write `tests/lib/cms/errors.test.ts` FIRST (rule #5), covering the behaviors above using the
    parse.test.ts structure. Then implement `errors.ts`:

    Export `CmsApiErrorSchema = z.discriminatedUnion('kind', [...])` with exactly the 5 literal
    kinds (D-01): `invalid_ccn`, `not_found` (+ `ccn: z.string()`), `network_error`,
    `cms_api_error`, `validation_error` — each with `message: z.string()`. `validation_error`
    gets NO extra field (D-05 — it must carry zero detail). Export `type CmsApiError = z.infer<...>`.
    Export `assertNever(x: never): never` that throws `'Unhandled CmsError kind: ' + JSON.stringify(x)`
    (D-03 exhaustiveness guard — a 6th kind added later without a switch case becomes a compile error
    in Phase 3). Export `class CmsError extends Error` with `constructor(public readonly kind: CmsApiError['kind'],
    message: string, public readonly extra?: Record<string, unknown>)` setting `this.name = 'CmsError'`
    (D-18 — enables `instanceof CmsError` catch so the route doesn't swallow unrelated errors).
    Use Zod v4 idioms (`.issues`, not `.errors`).
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/lib/cms/errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    `npx vitest run tests/lib/cms/errors.test.ts` passes. `errors.ts` exports `CmsApiErrorSchema`,
    `CmsApiError`, `CmsError`, `assertNever`. `CmsApiErrorSchema` parses all 5 valid kinds and
    rejects an unknown kind; `not_found` requires `ccn`; `validation_error` has no extra field.
    `new CmsError(...)` is `instanceof Error`.
  </acceptance_criteria>
  <done>
    The 5-kind discriminated union, `CmsError` class, and `assertNever` exist and are
    test-covered. `npm run verify` is green (no lint/format/typecheck regressions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constants → CMS fetch URL (Plan 02-02) | The host + dataset path originate ONLY from these constants; untrusted CCN never enters host/path |
| error union → Phase-3 client | The error `kind` set is the API↔UI contract; it must stay exhaustive and leak-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SSRF | Tampering | `constants.ts` CMS URL pieces | mitigate | Base URL + dataset id are fixed constants; the CCN is passed only as a query-condition value in Plan 02-02 (never concatenated into host/path). The 6-alphanumeric format gate (Plan 02-02) further constrains it. |
| T-02-LEAK | Information Disclosure | `errors.ts` `validation_error` variant | mitigate | `validation_error` schema variant carries NO extra field (only `kind`+`message`); the leak-prevention invariant is enforced by the route test in Plan 02-02 (D-05). |
| T-02-EXH | Tampering | `errors.ts` `kind` union | mitigate | `assertNever` guard turns an unhandled future kind into a compile error (D-03), preventing a silently dropped error case. |
| T-02-SC | Tampering | npm installs | accept | No new packages installed in this plan; all deps were legitimacy-gated in Phase 1 (01-01). No package-manager install tasks here. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (Task 1).
- `npx vitest run tests/lib/cms/errors.test.ts` green (Task 2).
- `npm run verify` green (typecheck → lint → format:check → test) before marking the plan done.
- Grep: `constants.ts` contains `4pq5-n9py` and `cms_certification_number_ccn`; `types.ts`
  contains none of the memory-sourced field names.
</verification>

<success_criteria>
- `constants.ts`, `types.ts`, `errors.ts` exist with the exact exports listed in `must_haves.artifacts`.
- The 5-kind discriminated union matches D-01 exactly; `not_found` carries `ccn`, `validation_error` carries no extra.
- `CmsError` is throwable/catchable via `instanceof`; `assertNever` enforces exhaustiveness.
- `FacilityData` is camelCase-only (D-14); every field traces to the fixture (rule #3).
- `npm run verify` is green.
</success_criteria>

<output>
Create `.planning/phases/02-api-routes-view-model-config/02-01-SUMMARY.md` when done.
</output>
