---
phase: 05-claims-based-metrics
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - medelite-report/src/lib/cms/types.ts
  - medelite-report/src/lib/cms/claims-mapper.ts
  - medelite-report/src/lib/cms/client.ts
  - medelite-report/tests/lib/cms/claims-mapper.test.ts
  - medelite-report/tests/lib/cms/client.test.ts
autonomous: true
requirements: [CLM-01, CLM-02, CLM-03]
must_haves:
  truths:
    - "joinClaimsAndAverages(claimsFixture, NATION, FL) returns exactly 12 HospMetric objects in the verbatim reference label order"
    - "Measure 521 facility value === 25.575578 (percent unit); 551 facility value === 2.752503 (rate unit); national/state avgs match the xcdc columns matched by description"
    - "A suppressed facility measure (adjusted_score '', footnote_for_score '9') → value null, footnoteCode '9'; its NATION/FL average rows STILL carry their numeric values (per-row suppression, D-10)"
    - "Fewer than 4 claims measures present (both fetches fulfilled) → joinClaimsAndAverages STILL returns the 12 HospMetric rows; the absent measure's facility row has value null + a fallback footnoteCode, while its NATION/FL average rows still carry their values (graceful partial — D-10/SC#5, no throw)"
    - "fetchClaimsMeasures returns 4 validated ClaimsRow for the 686123 fixture; fetchAverages returns { nation, state } AveragesRow with the SSRF/8s-timeout discipline of fetchFacility"
  artifacts:
    - path: "medelite-report/src/lib/cms/claims-mapper.ts"
      provides: "joinClaimsAndAverages(claimsRows, nationRow, stateRow) → HospMetric[] (always the 12 rows) + METRIC_DEFINITIONS"
      contains: "joinClaimsAndAverages"
    - path: "medelite-report/src/lib/cms/types.ts"
      provides: "HospMetric interface"
      contains: "interface HospMetric"
    - path: "medelite-report/src/lib/cms/client.ts"
      provides: "fetchClaimsMeasures(ccn), fetchAverages(state)"
      contains: "fetchClaimsMeasures"
  key_links:
    - from: "medelite-report/src/lib/cms/claims-mapper.ts"
      to: "medelite-report/src/lib/cms/averages-schema.ts"
      via: "description-substring key scan over AveragesRow (D-14, not hash slug)"
      pattern: "AVERAGE_COLUMN_DESCRIPTIONS"
    - from: "medelite-report/src/lib/cms/client.ts"
      to: "CMS_BASE_URL + DATASET_CLAIMS / DATASET_AVERAGES"
      via: "URL built from fixed constants; CCN/state only as conditions[][value] (SSRF discipline)"
      pattern: "AbortSignal.timeout"
---

<objective>
Build the join + fetch layer: the `HospMetric` domain type, the pure `joinClaimsAndAverages` mapper (the 12-row contract: verbatim labels, adjusted facility scores, description-matched averages, per-row suppression — including an absent measure rendered as a suppressed facility row while its averages still resolve, D-10/SC#5), and the two SSRF/timeout-disciplined sibling fetchers (`fetchClaimsMeasures`, `fetchAverages`). The mapper test is the executable spec for all three of CLM-01/02/03.

Purpose: This is the heart of the phase — the place where facility claims (`ijh5-nb2v`) join with NATION/state averages (`xcdc-v8bm`) into the 12 fixed data points (CLM-01), with the per-row suppression rule (CLM-02 / D-10) and the verbatim garbled labels (CLM-03 / D-04). It depends on Plan 01's schemas + constants and produces the `HospMetric[]` that Plan 03 threads into the view-model.

Output: 1 new mapper + 1 modified type file + 2 new fetchers, plus 1 new mapper test + 1 extended client test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-claims-based-metrics/05-CONTEXT.md
@.planning/phases/05-claims-based-metrics/05-RESEARCH.md
@.planning/phases/05-claims-based-metrics/05-PATTERNS.md
@.planning/phases/05-claims-based-metrics/05-VALIDATION.md
@CLAUDE.md
@.planning/phases/05-claims-based-metrics/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (this wave's dependency) + existing code. Use directly. -->

From Plan 01 (src/lib/cms/claims-schema.ts):
  export const ClaimsRowSchema  // .passthrough(); adjusted_score is number|null, footnote_for_score string
  export type ClaimsRow

From Plan 01 (src/lib/cms/averages-schema.ts):
  export const AveragesRowSchema  // .passthrough(); state_or_nation + processing_date typed; hash cols passthrough
  export type AveragesRow

From Plan 01 (src/lib/cms/constants.ts):
  DATASET_CLAIMS = "ijh5-nb2v", DATASET_AVERAGES = "xcdc-v8bm", AVERAGES_FILTER_FIELD = "state_or_nation"
Existing: CMS_BASE_URL, CCN_FILTER_FIELD

Existing fetchFacility pattern to mirror — src/lib/cms/client.ts lines 41–124:
  new URL(`${CMS_BASE_URL}/${DATASET}/0`); searchParams conditions[0][property|value|operator]="=";
  fetch with AbortSignal.timeout(8000); catch → CmsError("network_error"); !resp.ok → CmsError("cms_api_error");
  json.results array guard → CmsError("cms_api_error"); per-row Schema.safeParse.
  import { CmsError } from "@/lib/cms/errors";

Existing FacilityData has `state: string` (top-level) — fetchAverages receives this.

The 12-row contract (verbatim, D-04/D-06 — from 05-CONTEXT <specifics> / 05-RESEARCH METRIC_DEFINITIONS):
  1  "Short Term Hospitalization"                        521 facility percent
  2  "STR National Avg. for Hospitalization"             521 nation   percent
  3  "STR State National Avg. for Hospitalization"       521 state    percent  (garble: "National" spurious = STATE)
  4  "STR ED Visit"                                       522 facility percent
  5  "STR ED Visits National Avg."                        522 nation   percent
  6  "STR ED Visits State Avg."                           522 state    percent
  7  "LT Hospitalization"                                 551 facility rate
  8  "LT National Avg. for Hospitalization"               551 nation   rate
  9  "LT State National Avg. for Hospitalization"         551 state    rate     (same garble = STATE)
  10 "ED Visit"                                           552 facility rate     (garble: bare label = LT ED facility)
  11 "LT ED Visits National Avg."                         552 nation   rate
  12 "LT ED Visits State Avg."                            552 state    rate

Average-column description substrings (D-14, match by these, NOT the hash slug):
  521 → "rehospitalized"
  522 → "outpatient_em"
  551 → "hospitalizations_per_1000_longstay"
  552 → "outpatient_emergency_department_visits_per_1000_l"

686123 expected values (fixture-verified):
  facility: 521=25.575578, 522=8.094575, 551=2.752503, 552=0.910105
  NATION:   521=23.875617, 522=12.013574, 551=1.897659, 552=1.798049
  FL:       521=26.203324, 522=9.157686,  551=2.147753, 552=1.156036
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HospMetric type + build joinClaimsAndAverages mapper, driven by the 12-row mapper test (CLM-01/02/03)</name>
  <files>medelite-report/src/lib/cms/types.ts, medelite-report/src/lib/cms/claims-mapper.ts, medelite-report/tests/lib/cms/claims-mapper.test.ts</files>
  <read_first>
    - medelite-report/src/lib/cms/types.ts (the file being modified — FacilityData camelCase + JSDoc-to-CMS-source pattern)
    - medelite-report/src/lib/cms/mapper.ts (analog — pure CMS→domain transform; the only place CMS field names appear)
    - medelite-report/tests/lib/cms/mapper.test.ts (analog for the new mapper test — fixture import + inline synthetic rows for edge cases)
    - medelite-report/tests/fixtures/claims-686123.json (4-row source; all footnote_for_score "" → suppression needs a synthetic row)
    - medelite-report/tests/fixtures/averages-xcdc.json (object keyed NATION/FL; the two rows fed to the mapper)
    - .planning/phases/05-claims-based-metrics/05-PATTERNS.md (claims-mapper.ts section — METRIC_DEFINITIONS, AVERAGE_COLUMN_DESCRIPTIONS, joinClaimsAndAverages signature; claims-mapper.test.ts test-case list)
    - .planning/phases/05-claims-based-metrics/05-RESEARCH.md (Pattern 3 join + Open Question 1 partial-data resolution)
  </read_first>
  <behavior>
    - joinClaimsAndAverages(claimsFixture, averagesFixture.NATION, averagesFixture.FL) returns an array of exactly 12 HospMetric
    - The 12 labels appear in the exact verbatim order in the <interfaces> 12-row contract (garbles preserved: "STR State National Avg. for Hospitalization", bare "ED Visit")
    - Row 1 (521 facility): value === 25.575578, unit === "percent"; Row 7 (551 facility): value === 2.752503, unit === "rate"
    - Row 2 (521 nation): value === 23.875617; Row 3 (521 state/FL): value === 26.203324 (matched via "rehospitalized" description, not hash slug)
    - Row 11 (552 nation): value === 1.798049; Row 12 (552 state): value === 1.156036 (matched via "outpatient_emergency_department_visits_per_1000_l")
    - Suppressed facility measure (synthetic 521 row: adjusted_score "", footnote_for_score "9") → Row 1 value null + footnoteCode "9"; Row 2 (nation) + Row 3 (state) STILL carry 23.875617 / 26.203324 (per-row suppression, D-10)
    - Empty adjusted_score with footnote_for_score "" → value null, footnoteCode "" (formatFootnote will render "Not available")
    - Fewer than 4 claims measures (e.g. omit 552, both rows still given) → joinClaimsAndAverages STILL returns 12 HospMetric rows: Row 10 (552 facility) has value null + a fallback footnoteCode (formatFootnote renders "Not available"); Rows 11 & 12 (552 nation/state averages) STILL carry 1.798049 / 1.156036 — graceful partial per Open-Q1 (RESOLVED) / D-10 / SC#5, no throw
  </behavior>
  <action>
    In `types.ts` add the `HospMetric` interface (per 05-PATTERNS.md types.ts section): `label: string`, `value: number | null`, `unit: "percent" | "rate"`, `footnoteCode?: string`. Match the camelCase + JSDoc-source style; this is a domain type (no CMS snake_case leaks out of the mapper).

    Create `src/lib/cms/claims-mapper.ts` exporting a pure `joinClaimsAndAverages(claimsRows: ClaimsRow[], nationRow: AveragesRow, stateRow: AveragesRow): HospMetric[]` (always the 12 rows when given usable averages rows). Build the module-level `METRIC_DEFINITIONS` array (12 entries, verbatim labels + measureCode + source facility|nation|state + unit) from the <interfaces> 12-row contract — D-04 garbles preserved exactly. Build `AVERAGE_COLUMN_DESCRIPTIONS` (measureCode → description substring) from <interfaces>; resolve each average value by scanning the averages row keys for the one whose key CONTAINS the substring (D-14 runtime key scan — never hardcode the `_1d02`/`_d911`/`_de9d` slugs). Coerce the matched average string to number|null using the same empty→null logic as nullableNum (mirror it; reuse the inline helper from the schema files' pattern).

    Mapper logic: index claimsRows by measure_code. Always produce 12 HospMetric in METRIC_DEFINITIONS order — never `return undefined` for a partial claims set (Open-Q1 RESOLVED / D-10 / SC#5: fewer-than-4 is the per-row partial case, NOT a whole-section degrade; whole-section degrade is the route's `Promise.allSettled`-rejection decision in Plan 03, D-09). For each facility row: look up its claims row by measure_code — present → `adjusted_score` (number|null per D-15) + `footnote_for_score` → footnoteCode; ABSENT → value null + a footnoteCode that triggers the generic fallback (set it to the empty string `""` so formatFootnote renders "Not available", exactly like a suppressed score). Nation/state rows always take the description-matched average value (number|null) with no footnote. Per-row suppression (D-10): a suppressed OR absent facility row does NOT suppress its nation/state sibling rows. This file is the ONLY new place CMS field/column names appear (mirrors mapper.ts discipline).

    Create `tests/lib/cms/claims-mapper.test.ts` mirroring mapper.test.ts: import joinClaimsAndAverages + the two schemas + both fixtures (parse the fixtures through ClaimsRowSchema/AveragesRowSchema first, like the analog parses through the provider schema). Cover all behavior-block cases. Suppression + fewer-than-4 use inline synthetic rows (686123 has no suppression). No fenced code — build from the 05-PATTERNS.md claims-mapper sections.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/lib/cms/claims-mapper.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/cms/claims-mapper.test.ts` passes (from medelite-report/).
    - `joinClaimsAndAverages(claimsFixture, NATION, FL)` returns exactly 12 items in the verbatim label order (test asserts the full label array equals the 12-row contract, garbles included).
    - Facility/nation/state values for 521 and 552 match the fixture-verified numbers in <interfaces>; averages are resolved by description substring (test verifies a value would still resolve if the hash slug changed — e.g. by matching the substring, not the literal `_1d02`).
    - Suppressed synthetic 521 facility row → that row's value null + footnoteCode "9", while rows 2 & 3 keep their numeric averages.
    - Omitting one measure code (e.g. drop 552, both averages rows still supplied) → STILL returns 12 items: the dropped measure's facility row (Row 10) has value null + a fallback footnoteCode, and its nation/state rows (11 & 12) still carry their numeric averages — no thrown error (graceful partial, D-10/SC#5).
    - `types.ts` exports `interface HospMetric` with exactly the 4 fields; no CMS snake_case names leak into types.ts.
  </acceptance_criteria>
  <done>The mapper produces the 12 verbatim-labeled rows with correct 686123 values and per-row suppression — including a partial (fewer-than-4) claims set where the absent measure's facility row is suppressed but its averages still render (D-10/SC#5, no undefined return); the mapper test (CLM-01/02/03 spec) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add fetchClaimsMeasures + fetchAverages to client.ts with SSRF/8s-timeout discipline + extended client test</name>
  <files>medelite-report/src/lib/cms/client.ts, medelite-report/tests/lib/cms/client.test.ts</files>
  <read_first>
    - medelite-report/src/lib/cms/client.ts (the file being modified — fetchFacility is the exact pattern to replicate, lines 41–124)
    - medelite-report/src/lib/cms/errors.ts (CmsError constructor + kinds — import path @/lib/cms/errors)
    - medelite-report/tests/lib/cms/client.test.ts (analog/extend target — fetch-stub pattern with vi.stubGlobal)
    - medelite-report/tests/fixtures/claims-686123.json (the 4 rows the happy-path stub returns)
    - medelite-report/tests/fixtures/averages-xcdc.json (NATION/FL rows the averages stub returns)
    - .planning/phases/05-claims-based-metrics/05-PATTERNS.md (client.ts section — the differences-from-fetchFacility lists; SSRF + 8s-timeout Shared Pattern)
    - .planning/phases/05-claims-based-metrics/05-RESEARCH.md (Pattern 1 sibling fetcher; Pitfall 2 state-timing)
  </read_first>
  <behavior>
    - fetchClaimsMeasures("686123") (fetch stubbed with { results: claimsFixture }) → returns 4 validated ClaimsRow
    - fetchClaimsMeasures drops a row that fails ClaimsRowSchema (graceful partial via flatMap safeParse) and keeps valid rows; returns [] for 0 results
    - fetchClaimsMeasures throws CmsError("network_error") on fetch reject, CmsError("cms_api_error") on !resp.ok / non-array results
    - fetchAverages("FL") (fetch stubbed) → returns { nation: AveragesRow, state: AveragesRow }; uses AVERAGES_FILTER_FIELD, NOT a CCN filter
    - fetchAverages throws CmsError("network_error" / "cms_api_error") on the same failure modes
    - Both fetchers build the URL only from fixed constants; CCN / state pass only as conditions[][value] (SSRF — host/path never concatenated with input)
  </behavior>
  <action>
    Append `fetchClaimsMeasures(ccn: string): Promise<ClaimsRow[]>` and `fetchAverages(state: string): Promise<{ nation: AveragesRow; state: AveragesRow }>` to `client.ts`, importing `ClaimsRowSchema`/`ClaimsRow` and `AveragesRowSchema`/`AveragesRow` from the Plan-01 schema files and the new constants. Mirror fetchFacility's pipeline exactly (per 05-PATTERNS.md client.ts section): URL from `${CMS_BASE_URL}/${DATASET}/0`, `AbortSignal.timeout(8000)`, catch→`CmsError("network_error", "Claims data is unavailable.")` (averages: "State/national averages are unavailable."), `!resp.ok`/non-array→`CmsError("cms_api_error", ...)`.

    fetchClaimsMeasures: filter field `CCN_FILTER_FIELD`, value `ccn`, operator `"="`, `limit "10"`. Validate each result via `ClaimsRowSchema.safeParse(r)` inside `flatMap` (drop invalid rows — graceful partial, RESEARCH Pattern 1); return the array (may be empty — the route treats <4 as degraded, NOT a throw).

    fetchAverages: filter field `AVERAGES_FILTER_FIELD`. Fetch NATION + the given state. Either two parallel single-row fetches OR one fetch with an `IN`/two-value condition — prefer two `Promise.all` single-value fetches mirroring the fetchFacility shape for clarity; each value (`"NATION"`, state) goes ONLY into conditions[0][value] (SSRF — state is the Zod-validated FacilityData.state, never raw user input). Validate each row via `AveragesRowSchema.safeParse`; on either row missing/invalid → `CmsError("cms_api_error", ...)`. Return `{ nation, state }`.

    Extend `tests/lib/cms/client.test.ts` with describe blocks for both fetchers using the existing `vi.stubGlobal("fetch", ...)` pattern: happy path (4 rows / nation+state), reject → network_error, !resp.ok → cms_api_error, invalid-row-dropped for claims. No fenced code — build from the analog + 05-PATTERNS.md.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/lib/cms/client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/cms/client.test.ts` passes including the new fetchClaimsMeasures + fetchAverages blocks.
    - `client.ts` exports both fetchers; both use `AbortSignal.timeout(8000)` and build the URL from `CMS_BASE_URL` + the dataset constants (grep confirms `AbortSignal.timeout` appears for the new fetchers).
    - fetchClaimsMeasures returns a `ClaimsRow[]` (drops invalid rows, returns [] on zero results, never throws on a valid-but-empty response); throws CmsError on network/api failure.
    - fetchAverages returns `{ nation, state }` keyed off `AVERAGES_FILTER_FIELD` with no CCN filter; throws CmsError on failure.
    - No input (ccn/state) is concatenated into the URL host or path — only into `conditions[0][value]` (SSRF discipline, T-05-V5).
  </acceptance_criteria>
  <done>Both sibling fetchers replicate the fetchFacility SSRF/timeout/Zod discipline, return their typed shapes, throw CmsError on failure, and their client tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client.ts → CMS API | Outbound request; URL host/path from fixed constants, input only as a query condition value |
| CMS API → claims-mapper | Validated rows joined into HospMetric; column values number-coerced before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-V5-SSRF | Tampering | fetchAverages(state) URL build | mitigate | `state` is `FacilityData.state` (Zod-validated CMS field from fetchFacility), never raw user input. Host+path from `CMS_BASE_URL` + `DATASET_AVERAGES` constants; `state`/`"NATION"` go only into `conditions[0][value]` — never concatenated into host/path. Same discipline as fetchFacility (T-02-SSRF). |
| T-05-V5-DOS | Denial of Service | both fetchers | mitigate | `AbortSignal.timeout(8000)` caps upstream CMS latency on every fetch (mirrors D-19). A slow/hung CMS fails fast and degrades (route absorbs via allSettled in Plan 03). |
| T-05-COL | Tampering | claims-mapper average column scan | mitigate | Description-substring match (D-14) over the validated AveragesRow; matched value re-coerced number|null (mirrors nullableNum). A renamed column → null → per-row suppression text, never a crash or fabricated number. |
| T-05-SC | Tampering | npm installs | mitigate | No new packages this phase (RESEARCH audit empty). |
</threat_model>

<verification>
- `cd medelite-report && npx vitest run tests/lib/cms/` — schema + mapper + client suites green.
- Mapper returns 12 verbatim rows with fixture-verified values; suppression + fewer-than-4 covered.
- Both fetchers preserve SSRF/8s-timeout/Zod discipline.
</verification>

<success_criteria>
- joinClaimsAndAverages always produces the 12 fixed verbatim-labeled rows (CLM-01/CLM-03), with per-row suppression (CLM-02 / D-10) — a fewer-than-4 claims set degrades the absent facility row only (averages still render), graceful partial (SC#5), never undefined. Whole-section degrade (D-09) is the route's allSettled-rejection decision (Plan 03), not the mapper's.
- HospMetric domain type added; no CMS snake_case leaks beyond claims-mapper.ts.
- fetchClaimsMeasures + fetchAverages mirror the fetchFacility SSRF/timeout/Zod pipeline.
- Mapper + client tests green.
</success_criteria>

<output>
Create `.planning/phases/05-claims-based-metrics/05-02-SUMMARY.md` when done.
</output>
