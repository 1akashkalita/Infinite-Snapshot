---
phase: 05-claims-based-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - medelite-report/src/lib/cms/constants.ts
  - medelite-report/src/lib/cms/claims-schema.ts
  - medelite-report/src/lib/cms/averages-schema.ts
  - medelite-report/src/lib/report/format.ts
  - medelite-report/tests/lib/cms/claims-schema.test.ts
  - medelite-report/tests/lib/cms/averages-schema.test.ts
  - medelite-report/tests/lib/report/format.test.ts
autonomous: false
requirements: [CLM-02]
must_haves:
  truths:
    - "ClaimsRowSchema accepts a claims-686123.json row and coerces adjusted_score '' → null, '0' → 0, '25.575578' → 25.575578"
    - "ClaimsRowSchema rejects a row with a missing required key and rejects a non-numeric adjusted_score string"
    - "AveragesRowSchema passthrough preserves the hash-suffixed average columns so the mapper can key-scan them"
    - "formatFootnote('9') → 'Not reported (small sample)'; unknown/empty/undefined → 'Not available'"
  artifacts:
    - path: "medelite-report/src/lib/cms/claims-schema.ts"
      provides: "ClaimsRowSchema + ClaimsRow type for ijh5-nb2v rows"
      contains: "ClaimsRowSchema"
    - path: "medelite-report/src/lib/cms/averages-schema.ts"
      provides: "AveragesRowSchema + AveragesRow type for xcdc-v8bm rows"
      contains: "AveragesRowSchema"
    - path: "medelite-report/src/lib/cms/constants.ts"
      provides: "DATASET_CLAIMS, DATASET_AVERAGES, AVERAGES_FILTER_FIELD"
      contains: "DATASET_CLAIMS"
    - path: "medelite-report/src/lib/report/format.ts"
      provides: "formatFootnote(code) → suppression message"
      contains: "formatFootnote"
  key_links:
    - from: "medelite-report/src/lib/cms/claims-schema.ts"
      to: "tests/fixtures/claims-686123.json"
      via: "DATA-06-style every-key-exists assertion in claims-schema.test.ts"
      pattern: "claims-686123"
    - from: "medelite-report/src/lib/report/format.ts"
      to: "NH_Data_Dictionary Table 15 footnote codes"
      via: "FOOTNOTE_MESSAGES record"
      pattern: "FOOTNOTE_MESSAGES"
---

<objective>
Build the pure, validated CMS data primitives for Phase 5: the two new dataset constants, the two new Zod row schemas (`ClaimsRowSchema` for `ijh5-nb2v`, `AveragesRowSchema` for `xcdc-v8bm`), and the `formatFootnote` suppression-message helper — each with its alongside test (CLAUDE.md rule #5). Nothing fetches or joins yet; this plan produces the validators and the footnote map that Plans 02–04 build on.

Purpose: Rule #4 demands every CMS response is Zod-validated before it reaches render/export. These schemas are the validation gate for the two new datasets; `formatFootnote` is the render-time suppression handler for CLM-02. Isolating them (with tests) keeps the join/fetch plan focused and gives the suppression path a unit home (686123 has no suppressed values, so it MUST be exercised with a synthetic row here and in the mapper).

Output: 3 modified/new source files + 1 modified formatter, plus 3 test files (2 new, 1 extended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-claims-based-metrics/05-CONTEXT.md
@.planning/phases/05-claims-based-metrics/05-RESEARCH.md
@.planning/phases/05-claims-based-metrics/05-PATTERNS.md
@.planning/phases/05-claims-based-metrics/05-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Existing contracts the executor uses directly — no codebase exploration needed. -->

Existing `nullableNum` helper to mirror verbatim — src/lib/cms/schema.ts lines 23–39:
  union([z.string(), z.number(), z.null()]).transform: null→null, number→number,
  ""/"   "→null, "0"→0, non-numeric string → ctx.addIssue + z.NEVER (rejected).
  It is NOT exported from schema.ts today. Replicate it inline in each new schema file
  (copy the helper into claims-schema.ts and averages-schema.ts) — do NOT export/import
  across files unless you also add the export; mirroring inline matches 05-PATTERNS.md.

Existing constants.ts already exports (with JSDoc traceability):
  CMS_BASE_URL = "https://data.cms.gov/provider-data/api/1/datastore/query"
  DATASET_PROVIDER_INFO = "4pq5-n9py"
  CCN_FILTER_FIELD = "cms_certification_number_ccn"

Existing format.ts exports (null-safe, === null semantics, PLACEHOLDER="N/A" private):
  formatRating, formatBeds, formatPercent (1 dp), formatRate (2 dp), formatLocation, formatDate

claims-686123.json row shape (verified) — top-level keys present:
  cms_certification_number_ccn, provider_name, provider_address, citytown, state, zip_code,
  measure_code ("521"|"522"|"551"|"552"), measure_description, resident_type,
  adjusted_score, observed_score, expected_score, footnote_for_score (""), 
  used_in_quality_measure_five_star_rating, measure_period, location, processing_date

averages-xcdc.json shape (verified) — an object keyed "NATION" / "FL"; each value is one row:
  state_or_nation, processing_date, plus ~45 hash-suffixed metric columns, including the 4 the
  mapper needs (matched by description substring in Plan 02, not keyed here):
    percentage_of_short_stay_residents_who_were_rehospitalized__1d02
    percentage_of_short_stay_residents_who_had_an_outpatient_em_d911
    number_of_hospitalizations_per_1000_longstay_resident_days
    number_of_outpatient_emergency_department_visits_per_1000_l_de9d
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the 3 claims/averages dataset constants + ClaimsRowSchema + averages-schema, with the two schema test files</name>
  <files>medelite-report/src/lib/cms/constants.ts, medelite-report/src/lib/cms/claims-schema.ts, medelite-report/src/lib/cms/averages-schema.ts, medelite-report/tests/lib/cms/claims-schema.test.ts, medelite-report/tests/lib/cms/averages-schema.test.ts</files>
  <read_first>
    - medelite-report/src/lib/cms/constants.ts (the file being modified — existing 3 constants + JSDoc traceability pattern)
    - medelite-report/src/lib/cms/schema.ts (analog — nullableNum lines 23–39, .passthrough() object pattern lines 41–68)
    - medelite-report/tests/lib/cms/schema.test.ts (analog for both new test files — happy path, empty→null, real-0, missing-key-fails, non-numeric-fails, DATA-06 every-key-exists)
    - medelite-report/tests/fixtures/claims-686123.json (field-name + value anchor for ClaimsRowSchema — rule #3)
    - medelite-report/tests/fixtures/averages-xcdc.json (field-name anchor for AveragesRowSchema — rule #3; object keyed NATION/FL)
    - .planning/phases/05-claims-based-metrics/05-PATTERNS.md (claims-schema / averages-schema pattern sections + Shared Patterns: nullableNum, isolatedModules export, Zod v4 result.error.issues)
  </read_first>
  <behavior>
    - ClaimsRowSchema.safeParse(claimsFixture[0]) succeeds; measure_code === "521"; typeof adjusted_score === "number" (25.575578)
    - adjusted_score "" → null; adjusted_score "0" → 0; non-numeric adjusted_score (e.g. "abc") → safeParse success === false
    - footnote_for_score is preserved as a string ("" passes; "9" passes)
    - A row missing a required key (e.g. measure_code) → safeParse success === false
    - DATA-06 guard: every key in ClaimsRowSchema.shape exists in claimsFixture[0]
    - AveragesRowSchema.safeParse(averagesFixture.NATION) succeeds; state_or_nation === "NATION" preserved
    - Passthrough holds: result.data["percentage_of_short_stay_residents_who_were_rehospitalized__1d02"] is present (not stripped)
    - A row missing state_or_nation → safeParse success === false
  </behavior>
  <action>
    [BLOCKING — rule #3] Before writing the dataset-ID constants, re-confirm `ijh5-nb2v` and `xcdc-v8bm` are still live via the CMS metastore (Task 1a is the blocking checkpoint below; do not hardcode until it passes). Per D-16, IDs rotate.

    In `constants.ts` (D-16, per 05-PATTERNS.md "constants.ts" section): append three exports after the existing three, each with the same JSDoc-traceability comment style — `DATASET_CLAIMS = "ijh5-nb2v"`, `DATASET_AVERAGES = "xcdc-v8bm"`, `AVERAGES_FILTER_FIELD = "state_or_nation"`. Trace each to the metastore confirmation date + the captured fixture.

    Create `src/lib/cms/claims-schema.ts`: copy the `nullableNum` helper from schema.ts lines 23–39 verbatim (inline, not imported), then export `ClaimsRowSchema = z.object({...}).passthrough()` with required string fields `cms_certification_number_ccn`, `measure_code`, `measure_description`, `resident_type`, `footnote_for_score`, `processing_date`, and `adjusted_score: nullableNum`. Export `type ClaimsRow = z.infer<typeof ClaimsRowSchema>`. Do NOT model `observed_score`/`expected_score` (D-15 — adjusted only; they pass through). Field names traced to claims-686123.json (rule #3).

    Create `src/lib/cms/averages-schema.ts`: same `nullableNum` inline helper, then export `AveragesRowSchema = z.object({ state_or_nation: z.string(), processing_date: z.string() }).passthrough()` and `type AveragesRow`. CRITICAL (D-14 / RESEARCH Pitfall 1): do NOT declare the hash-suffixed average columns as keys — `.passthrough()` keeps them; the Plan-02 mapper resolves them by description substring at runtime.

    Create `tests/lib/cms/claims-schema.test.ts` and `tests/lib/cms/averages-schema.test.ts` mirroring schema.test.ts structure. Use Zod v4 `result.error.issues` (NOT `.errors`). Synthetic suppressed/malformed rows are inline typed constants (like schema.test.ts D-12 pattern), not fixtures.

    No fenced code in this plan — build straight from the 05-PATTERNS.md "claims-schema.ts" / "averages-schema.ts" sections which carry the exact shapes.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/lib/cms/claims-schema.test.ts tests/lib/cms/averages-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/cms/claims-schema.test.ts tests/lib/cms/averages-schema.test.ts` passes (from medelite-report/).
    - `constants.ts` exports `DATASET_CLAIMS === "ijh5-nb2v"`, `DATASET_AVERAGES === "xcdc-v8bm"`, `AVERAGES_FILTER_FIELD === "state_or_nation"`, each with a traceability JSDoc comment.
    - `claims-schema.ts` exports `ClaimsRowSchema` (with `.passthrough()`) and `ClaimsRow`; `adjusted_score` uses the inline `nullableNum`; `observed_score`/`expected_score` are NOT declared keys.
    - `averages-schema.ts` exports `AveragesRowSchema` (with `.passthrough()`) and `AveragesRow`; only `state_or_nation` + `processing_date` are declared keys; no hash-suffixed columns declared.
    - claims-schema test asserts every `ClaimsRowSchema.shape` key exists in `claims-686123.json[0]` (DATA-06 style).
    - Tests use `result.error.issues` (Zod v4), not `.errors`.
  </acceptance_criteria>
  <done>Both new schemas validate their fixtures, coerce/reject per the nullableNum contract, preserve passthrough columns, and the two new test files are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1a: [BLOCKING] Re-confirm dataset IDs ijh5-nb2v + xcdc-v8bm via the CMS metastore (rule #3 / D-16)</name>
  <files>medelite-report/src/lib/cms/constants.ts</files>
  <action>
    Before this checkpoint, the executor runs the two read-only public-API curl calls below to re-confirm the dataset IDs are still live and carry the expected fields, then writes the confirmed IDs into constants.ts (Task 1). This checkpoint is the human gate that the re-confirmation actually happened (rule #3 traceability) — it is NEVER auto-approvable regardless of workflow.auto_advance.
  </action>
  <what-built>
    The executor queried the CMS metastore to re-confirm both dataset IDs are still live and map to the expected datasets BEFORE hardcoding them in constants.ts. Distribution/dataset IDs rotate (CLAUDE.md rule #3); RESEARCH confirmed them live 2026-06-18 but this re-confirms at implementation time. Suggested commands (read-only public API):
    - `curl -s "https://data.cms.gov/provider-data/api/1/datastore/query/ijh5-nb2v/0?limit=1"` → expect HTTP 200 with a row carrying `cms_certification_number_ccn`, `measure_code`, `adjusted_score`, `footnote_for_score`.
    - `curl -s "https://data.cms.gov/provider-data/api/1/datastore/query/xcdc-v8bm/0?conditions[0][property]=state_or_nation&conditions[0][value]=NATION&conditions[0][operator]==&limit=1"` → expect HTTP 200 with a row carrying `state_or_nation` and the 4 average columns (matched by description).
  </what-built>
  <how-to-verify>
    1. Confirm both curl calls returned HTTP 200 with the expected field names (not a 404 / empty / rotated-ID error).
    2. If an ID has rotated: re-resolve via `https://data.cms.gov/provider-data/api/1/metastore/schemas/dataset/items` (match by dataset title "Medicare Claims Quality Measures" / "State US Averages"), update the constant + the RESEARCH/PATTERNS note, and re-run Task 1.
    3. This gate is NEVER auto-approvable (rule #3 traceability). Type "approved" only after the two responses are confirmed live, or describe the rotated IDs.
  </how-to-verify>
  <verify>
    <automated>MANUAL — blocking human checkpoint; the two curl calls return HTTP 200 with the expected fields before constants are hardcoded.</automated>
  </verify>
  <acceptance_criteria>
    - Both dataset-ID curl calls returned HTTP 200 with the expected fields (or the rotated IDs were resolved + applied).
    - The IDs written into constants.ts match the re-confirmed live IDs.
  </acceptance_criteria>
  <done>Dataset IDs ijh5-nb2v + xcdc-v8bm re-confirmed live (or corrected) before being hardcoded — rule #3 satisfied.</done>
  <resume-signal>Type "approved" (IDs confirmed live) or paste the corrected dataset IDs.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add formatFootnote(code) to format.ts with the full Table-15 message map + extended format tests</name>
  <files>medelite-report/src/lib/report/format.ts, medelite-report/tests/lib/report/format.test.ts</files>
  <read_first>
    - medelite-report/src/lib/report/format.ts (the file being modified — existing null-safe formatters, === null discipline, private PLACEHOLDER)
    - medelite-report/tests/lib/report/format.test.ts (analog/extend target — existing describe blocks for formatPercent/formatRate)
    - .planning/phases/05-claims-based-metrics/05-RESEARCH.md (Footnote Codes table + formatFootnote Code Example)
    - .planning/phases/05-claims-based-metrics/05-PATTERNS.md (format.ts section — FOOTNOTE_MESSAGES record + the 9-case test block)
    - CLAUDE.md (rule #3 — footnote codes trace to NH_Data_Dictionary Table 15 via FEATURES.md)
  </read_first>
  <behavior>
    - formatFootnote("9")  → "Not reported (small sample)"
    - formatFootnote("7")  → "Not available"
    - formatFootnote("10") → "Not submitted"
    - formatFootnote("1")  → "Not enough data"
    - formatFootnote("2")  → "Not enough data"
    - formatFootnote("28") → "Not enough data (annual measure)"
    - formatFootnote("99") → "Not available" (unknown-code fallback)
    - formatFootnote("")   → "Not available" (empty-score, no-footnote case)
    - formatFootnote(undefined) → "Not available"
  </behavior>
  <action>
    Append `formatFootnote(footnoteCode: string | undefined): string` to `format.ts` (D-11). Add a module-private `FOOTNOTE_MESSAGES: Record<string, string>` mapping codes 1/2/7/9/10/28 to the exact strings in the behavior block, and a `FOOTNOTE_FALLBACK = "Not available"`. Return the fallback for falsy/empty code and for any unknown code (`?? FOOTNOTE_FALLBACK`). Add a JSDoc comment tracing the codes to NH_Data_Dictionary Table 15 (via FEATURES.md) per rule #3. Match the existing single-line, null-safe formatter style (no fenced code here — build from the 05-PATTERNS.md format.ts section).

    Extend `tests/lib/report/format.test.ts` with a new `describe("formatFootnote", ...)` block covering all 6 known codes + unknown ("99") + empty ("") + undefined — the 9 cases in the behavior block. Do NOT modify the existing formatPercent/formatRate describe blocks.
  </action>
  <verify>
    <automated>cd medelite-report && npx vitest run tests/lib/report/format.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/lib/report/format.test.ts` passes with the new formatFootnote describe block.
    - `format.ts` exports `formatFootnote`; the 6 known codes + unknown + empty + undefined all return the exact strings in the behavior block.
    - The footnote map carries a JSDoc traceability comment to Table 15 (rule #3).
    - Existing formatPercent/formatRate tests still pass (no regression).
  </acceptance_criteria>
  <done>formatFootnote maps all known codes, falls back safely for unknown/empty/undefined, and its tests are green alongside the unchanged existing formatter tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CMS API → claims/averages schema | Untrusted upstream JSON crosses into the app; validated here before any consumer touches it (rule #4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-V5-NUM | Tampering | `ClaimsRowSchema.adjusted_score` (nullableNum) | mitigate | nullableNum rejects non-numeric/non-string/non-null values (Number.isFinite gate → ctx.addIssue → safeParse fails); malformed scores can never be coerced into a fabricated number. Tested with a non-numeric synthetic row. |
| T-05-V5-PASS | Tampering | `AveragesRowSchema.passthrough()` | accept | Passthrough preserves ~45 unmodeled columns by design (D-14 runtime key scan needs them). Only `state_or_nation`/`processing_date` are typed; the mapper (Plan 02) reads only the 4 description-matched columns. No untrusted column is executed or rendered raw — values are number-coerced and formatted downstream. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit is empty). No install task → no slopcheck gate needed. |
</threat_model>

<verification>
- `cd medelite-report && npx vitest run tests/lib/cms/ tests/lib/report/` — schema + format suites green.
- Dataset IDs re-confirmed live (Task 1a blocking checkpoint approved).
- No new packages added (RESEARCH audit empty).
</verification>

<success_criteria>
- ClaimsRowSchema + AveragesRowSchema validate their fixtures and enforce the nullableNum / required-key / passthrough contracts (rule #4).
- formatFootnote covers all 6 Table-15 codes + safe fallbacks (CLM-02 suppression-message foundation).
- The 3 constants exist and are traced to a re-confirmed metastore lookup (rule #3 / D-16).
- All three test files green; no regression in existing schema/format tests.
</success_criteria>

<output>
Create `.planning/phases/05-claims-based-metrics/05-01-SUMMARY.md` when done.
</output>
